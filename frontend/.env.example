# Backend API URL — paste the URL of your deployed asscher-ai-backend service.
# On Vercel: Project → Settings → Environment Variables → add VITE_API_URL, then redeploy.
# (Alternative that needs no rebuild: edit public/config.js instead.)
VITE_API_URL=http://localhost:4000
